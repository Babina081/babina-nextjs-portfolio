import React from "react";

const Loading = () => {
  return (
    <div className="spinner w-screen h-screen items-center justify-center flex m-auto bg-gradient-to-b from-black to-blue-500 dark:from-white dark:to-blue-500 relative z-50"></div>
  );
};

export default Loading;
